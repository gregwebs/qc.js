define('generator/html/__all__', [
  'generator/html/color'
], function(color) {

  // Populate the following using their namespaces.
  // So they will be reachable via 'qs.generator.html'
  var exports = {
    color: color
  };

  return exports;
});
